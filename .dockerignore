# =============================================================================
# Docker 构建上下文排除清单
#
# 为什么必须有这个文件：
#   项目根目录下的 .venv/ 实测 397 MB。docker build 会先把「构建上下文」
#   （也就是整个项目目录）打包发给 Docker 引擎，不排除的话每次构建都要
#   白白传输几百 MB，慢且毫无意义。web/node_modules 同理。
#
# 注意 .dockerignore 的匹配规则：
#   模式是相对「构建上下文根目录」的，`*.db` 只匹配根目录下的 .db 文件，
#   要匹配任意层级必须写 `**/*.db`。下面两种都写了。
# =============================================================================

# --- Python 虚拟环境（最大的一块，397 MB）---------------------------------
.venv/
venv/
env/
ENV/

# --- Python 缓存 -----------------------------------------------------------
__pycache__/
**/__pycache__/
*.py[cod]
**/*.py[cod]
*.egg-info/
**/*.egg-info/
.eggs/
build/
dist/

# --- 工具缓存 --------------------------------------------------------------
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# --- 版本控制 --------------------------------------------------------------
.git/
.gitignore
.gitattributes

# --- 前端（后端镜像完全用不到，node_modules 又特别大）---------------------
web/

# --- 文档 / 技术验证 / 测试（不进生产镜像）--------------------------------
docs/
spikes/
tests/
*.md
!README.md

# --- 本地数据（SQLite 主库 1.2 GB，绝不能进镜像）-------------------------
*.db
**/*.db
*.db-wal
**/*.db-wal
*.db-shm
**/*.db-shm
*.parquet
**/*.parquet
*.log
**/*.log
.Kuantix/
data/

# --- 容器与部署产物本身 ----------------------------------------------------
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.yaml

# --- 操作系统 / 编辑器 -----------------------------------------------------
.DS_Store
**/.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# --- 环境变量文件（防止把本地密钥/地址误打进镜像）-------------------------
.env
.env.*
