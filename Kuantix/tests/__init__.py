"""Kuantix 验收测试包（T01 + T02 批次 1）。

五条验收线（对应任务书的五个正确性陷阱）：

===  ==========================================  ==============================
序号  验收内容                                    测试文件
===  ==========================================  ==============================
①    系数表是 import 引用而非复制（NF-25/T1）     ``test_a1_coefficient_reference.py``
②    UNKNOWN 类型写入被拒绝并进隔离区（T1）        ``test_a2_unknown_quarantine.py``
③    vol ÷100 不溢出 + 越界样本被拦截（T2/T3）     ``test_a3_vol_unit_and_uint32.py``
④    枚举每页新建连接 <1s/页（RD-10/T4）           ``test_a4_universe_connection.py``
⑤    写→回读一致，覆盖 ≥4 种证券类型（NF-27）      ``test_a5_write_readback.py``
===  ==========================================  ==============================

另有 ``test_t01_skeleton.py`` 覆盖 T01 的骨架验收（CLI / serve / 配置）。

所有测试**默认离线可复现**：需要外网 TDX 服务器的用例统一打
``@pytest.mark.network``，离线环境自动跳过（见 ``conftest.py``）。
"""

__all__: list[str] = []
