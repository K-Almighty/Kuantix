# =============================================================================
# R4 fail-loud 豁免清单（NF-26）
# =============================================================================
# 约束：PRD §5.0「NF-26 是全项目通用原则，优先级高于本章其余所有条目」
#       「严禁静默兜底、静默降级、静默使用默认值」
#
# 格式（三段，冒号分隔）：
#     文件路径:行号:豁免理由
#
#   - 文件路径：相对项目根（.../Kuantix/），支持 fnmatch 通配，例：
#         Kuantix/config.py
#         Kuantix/api/routers/*.py
#   - 行号：具体行号；写 `*` 表示整文件豁免（**强烈不建议**，
#           会在 terminal summary 里被单独标出，且上限 3 条）
#   - 理由：>= 6 个字，必须说清「为什么这里静默取默认值是安全的」。
#           只写「配置读取」不够，要写「取不到时的默认值不承载业务语义，
#           且已在 X 处做过 require_known 校验」这种级别。
#
# 审批要求：新增豁免必须经 team-lead 评审。写不出理由的，就是该改代码。
#
# 未命中的豁免条目会在报告里列为「建议清理」——代码改了就把豁免删掉，
# 别让 allowlist 变成僵尸清单。
# =============================================================================

# ---- 当前为空：T01/T02 代码尚未落地，零豁免 ----
# 预期会需要豁免的典型场景（等代码落地后由工程师提 PR、team-lead 审）：
#   Kuantix/config.py:NN:toml 可选配置项，缺省值来自 config.toml 模板且已在 schema 层校验
#   Kuantix/core/plugins.py:NN:插件元数据可选描述字段，缺失不影响注册正确性

# ===== team-lead 裁决 1 / 方案 C（2026-08-01 审批通过）=====
# 由 test_r4_eventbus_guard_present 守卫断言担保：publish/subscribe 两侧均已
# require_known 校验主题合法性，此处 .get(key, ()) 仅表示「已声明主题的零订阅
# 合法态」，拼错主题名会在守卫处立刻报错，不再静默落空（NF-26 头号场景已封死）。
Kuantix/core/eventbus.py:247:已声明主题零订阅合法态；publish 侧 require_known 已前置校验（见 test_r4_eventbus_guard_present）
Kuantix/core/eventbus.py:274:已声明主题零订阅合法态；subscriber_count 仅统计已知主题订阅者（见 test_r4_eventbus_guard_present）
