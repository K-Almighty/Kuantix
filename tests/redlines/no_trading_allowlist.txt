# =============================================================================
# R5 无下单接口豁免清单（NF-21）
# =============================================================================
# 约束：PRD NF-21「代码库中不得包含任何券商交易接口、委托下单、资金划转相关
#       实现或依赖。导出清单为纯文本文件产物。」
#       PRD Q2「不做订单状态机、不做资金校验、不触碰真实下单接口。」
#
# 格式：文件路径:行号:豁免理由
#
# ⚠ 本清单原则上应当**永远为空**。
#   NF-21 是产品范围边界，不是工程取舍——需要豁免时，先问清楚是不是范围跑偏了。
#   检查器已经内置了两类不误伤的设计，正常情况下不需要豁免：
#     1. 只看「函数/方法定义名」和「REST 路由路径」，不扫数据结构字段名
#        （所以 ScreenResult.direction = "buy" 这类不会命中）
#     2. 路径含 backtest/simul/paper/mock/replay 的文件使用收窄词表
#        （放行回测撮合惯用的 buy/sell/order_target 等命名）
#
#   若确需豁免（例如 P1「下单清单导出器」里有个方法叫 export_buy_list），
#   优先考虑改名（→ export_target_list），改不动再登记，并注明该实现
#   「只产出纯文本文件，不发起任何网络交易请求」。
# =============================================================================

# ---- 当前为空 ----
